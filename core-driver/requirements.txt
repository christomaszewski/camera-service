# Python deps installed via pip inside the container.
#
# IMPORTANT: PyGObject (gi), GStreamer bindings, and the Aravis bindings come
# from the system / container (apt + the Aravis source build in the Dockerfile),
# NOT from pip. Do not add pygobject / gstreamer here.
PyYAML>=6.0
# The lifecycle control plane (docs/LIFECYCLE.md). Same pin as plugins/webrtc-bridge: one zenoh minor
# per vehicle. Optional at runtime -- a missing binding degrades to signal-only control.
eclipse-zenoh==1.9.0
